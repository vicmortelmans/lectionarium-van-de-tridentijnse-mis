The source file `missaal-eo.markdown` is generated elsewhere. It contains the whole content. It is split on 2nd-level headers using `mdsplit` (`python -m pip install mdsplit`).

`mdsplit -l 2 -t -o docs missaal-eo.markdown`
